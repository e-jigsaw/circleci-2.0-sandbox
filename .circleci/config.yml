version: 2
jobs:
  build:
    docker:
      - image: node:7
    working_directory: /home/ubuntu/circleci-2.0-sandbox
    steps:
      - checkout
      - run:
          name: sample
          command: node -v
